# Core dependencies
dingtalk-stream>=0.24.2
python-dotenv>=1.0.0
aiohttp>=3.8.0
requests>=2.31.0

# AI/LLM dependencies (only used in tools.py, not in main flow)
langchain>=0.1.0

# Security and certificates
certifi>=2023.7.22

# Development and debugging
colorama>=0.4.6 